# Stripped-down dependencies for Streamlit Cloud (dashboard only).
# No torch/transformers/lightgbm — avoids OOM on free tier.

streamlit
altair<5
pandas>=1.5.0
plotly
sqlalchemy>=1.4.0
mysql-connector-python
python-dotenv
redis
